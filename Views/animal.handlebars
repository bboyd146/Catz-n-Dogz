<section class="container">
    <h2>hi there</h2>
    <h3 class="gallery-name">{{animal.animal_name}}</h3>
    <section class="flex-row justify-center">
        {{#each animal.pets as |pet|}}
        {{> pet-details}}
        {{/each}}
    </section>
</section>